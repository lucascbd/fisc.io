-- Seed inicial: categories e ipca
-- Este arquivo é carregado automaticamente no primeiro deploy (quando as tabelas estiverem vazias).
-- Em redeploys com dados existentes, este arquivo NÃO é executado.

-- Insira abaixo os dados de categorias e ipca no formato INSERT:

-- CATEGORIES
-- INSERT INTO categories (id, name, description, icon, color, is_active, display_order, ipca_category_code, ipca_category_name, created_at) VALUES
-- (1, 'Alimentação', '', '🍴', '#FF6B6B', true, 1, NULL, NULL, NOW()),
-- ...;

-- IPCA
-- INSERT INTO ipca (...) VALUES (...);
